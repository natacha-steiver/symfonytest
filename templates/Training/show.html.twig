{% extends "base.html.twig" %}

{% block title %}{{ training.title }}{% endblock %}
 {% block body %}
 <section id="training" class="container">
 <!--detail-->


<h4 class="">{{ training.title }}</h4>
<div class="row">

    <div class="col-lg-6">{{ training.description }}</div>
    <div class="col-lg-6 space-mobile" > <img style="max-width:100%;" src={{asset('images/'~training.imageName) }} /></div>
</div>

</section>

 {% endblock %}
 